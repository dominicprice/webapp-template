project_name:
    type: str
    help: What is the name of the webapp?

project_slug:
    type: str
    help: What is the slug of the webapp (used for target names)?
    default: "{{ project_name.lower().replace(' ', '_') }}"

project_description:
    type: str
    help: Give a brief description of the project

author:
    type: str
    help: What is your name?
